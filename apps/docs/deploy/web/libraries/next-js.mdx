---
title: Next.js
---

This lib is a convenient wrapper around the [Mozbot React](./react) library. It makes sure the components are lazy loaded.

## Install

```bash
npm install @mozbot.io/js @mozbot.io/nextjs
```

## Usage

Use it like you would use the [Mozbot React](./react) library.
